// extern crate afl;
//
// use std::env;
// use std::fs;
// use std::io::Read;
//
// extern crate ecl;
// extern crate serde;
//
// use serde::Serialize;

fn main() {
	// afl::handle_str(|s| {
	// 	let val = ecl::parse(s).unwrap();
	// 	let mut buf = String::new();
	// 	val.serialize(&mut ecl::lines::Serializer::new(&mut buf)).unwrap();
	// });
}
